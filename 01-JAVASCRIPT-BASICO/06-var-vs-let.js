var camper = 'Isaac';
var camper = 'Jose';

console.log(camper);

let camper2 = 'Isaac';
let camper2 = 'Jose'; //da error porque no se puede declarar dos veces, ni mas
console.log(camper2);


// Así que la diferencia de var con let:

// con var una variable con el mismo nombre se puede declarar varias veces.
// con let una variable con el mismo nombre no se puede declarar varias veces.
// una variable con el mismo nombre solo puede declararse una vez.